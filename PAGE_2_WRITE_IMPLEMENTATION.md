# PAGE 2 - WRITE 实现文档

## 🎯 功能概览

Page 2 - Write 页面用于收集职位的详细描述信息和公司品牌资料。包含 5 个主要功能需求（FR-W1 到 FR-W5）。

---

## ✅ 功能需求实现

### FR-W0: Company Name ⭐ **必填**

```
┌─────────────────────────────────────────┐
│ Company Name *                           │
│ [Enter your company name__________]     │
└─────────────────────────────────────────┘
```

**字段**：
- 类型：文本输入
- 必填：是
- 验证：不能为空
- 占位符："Enter your company name"

---

### FR-W1: Job Description ⭐ **必填**

```
┌─────────────────────────────────────────┐
│ Job Description *                        │
│ ℹ️ Provide a detailed description...    │
│                                          │
│ ┌──────────────────────────────────────┐│
│ │ Describe the role in detail...       ││
│ │                                       ││
│ │ [Large text area - 12 rows]          ││ ← Rich textarea
│ │                                       ││
│ │                                       ││
│ │                                       ││
│ └──────────────────────────────────────┘│
│                          2,450 characters│ ← 字符计数
└─────────────────────────────────────────┘
```

**字段**：
- 类型：大型文本区域（Textarea）
- 必填：是
- 最小高度：300px（12行）
- 功能：可调整大小、字符计数
- 验证：不能为空

**特点**：
- ✅ 富文本样式（较大字体、行高1.8）
- ✅ 可垂直调整大小
- ✅ 实时字符计数
- ✅ 清晰的占位符提示

---

### FR-W2: Job Summary ⭐ **必填**

```
┌─────────────────────────────────────────┐
│ Job Summary *                            │
│ ℹ️ A short, compelling summary...       │
│ (150-200 characters recommended)        │
│                                          │
│ ┌──────────────────────────────────────┐│
│ │ e.g., Join our AI team to build     ││
│ │ cutting-edge machine learning...     ││ ← 3 行文本框
│ └──────────────────────────────────────┘│
│                          187 / 250 chars│ ← 字符限制
└─────────────────────────────────────────┘
```

**字段**：
- 类型：文本区域（Textarea）
- 必填：是
- 行数：3行
- 最大长度：250字符
- 推荐长度：150-200字符

**特点**：
- ✅ 字符计数：显示 "X / 250 characters"
- ✅ 硬限制 250 字符
- ✅ 用于搜索结果摘要
- ✅ 清晰的使用提示

---

### FR-W3: Key Selling Points 🔶 **可选**

```
┌─────────────────────────────────────────────────────────┐
│ Key Selling Points                                       │
│ Highlight 3 key benefits or unique aspects (optional)   │
│                                                          │
│ ┌────────────────┐ ┌────────────────┐ ┌──────────────┐│
│ │ Selling Point 1│ │ Selling Point 2│ │ Selling Pt 3 ││
│ │                │ │                │ │              ││
│ │ [Competitive  ]│ │ [Cutting-edge ]│ │ [Flexible   ]││
│ │  salary +     │ │  AI projects  │ │  remote work ││
│ │  equity____]  │ │  _________]   │ │  policy____] ││
│ └────────────────┘ └────────────────┘ └──────────────┘│
└─────────────────────────────────────────────────────────┘
```

**字段**：
- 类型：3个文本输入框（网格布局）
- 必填：否（全部可选）
- 布局：响应式网格（auto-fit, minmax(250px, 1fr)）

**示例**：
1. "Competitive salary + equity"
2. "Work on cutting-edge AI projects"
3. "Flexible remote work policy"

**特点**：
- ✅ 响应式布局（桌面3列，移动1列）
- ✅ 清晰的占位符示例
- ✅ 独立可选字段

---

### FR-W4: Company Branding 🔶 **可选**

#### 4.1 Company Logo Upload

```
未上传状态:
┌─────────────────────────────────────────┐
│ Company Logo                             │
│ ℹ️ Square image, at least 200x200px     │
│                                          │
│ ┌──────────────────────────────────────┐│
│ │          📤                           ││
│ │    Click to upload logo              ││ ← 上传区域
│ │    or drag and drop                  ││
│ └──────────────────────────────────────┘│
└─────────────────────────────────────────┘

已上传状态:
┌─────────────────────────────────────────┐
│ ┌──────────────────────────────────────┐│
│ │                                   [×] ││ ← 删除按钮
│ │       [Company Logo Preview]         ││
│ │        200x200 居中显示              ││ ← Logo预览
│ │                                       ││
│ └──────────────────────────────────────┘│
└─────────────────────────────────────────┘
```

**规格**：
- 推荐尺寸：正方形，至少 200x200px
- 支持格式：PNG, JPG, GIF, WebP
- 文件大小：最大 5MB
- 显示方式：居中，contain模式

**验证**：
- ✅ 文件类型检查
- ✅ 文件大小限制
- ✅ 实时预览

---

#### 4.2 Cover Image Upload

```
未上传状态:
┌─────────────────────────────────────────┐
│ Cover Image                              │
│ ℹ️ 1200x400px landscape image          │
│                                          │
│ ┌──────────────────────────────────────┐│
│ │          🖼️                          ││
│ │    Click to upload cover image       ││ ← 上传区域
│ │    or drag and drop                  ││
│ └──────────────────────────────────────┘│
└─────────────────────────────────────────┘

已上传状态:
┌─────────────────────────────────────────┐
│ ┌──────────────────────────────────────┐│
│ │                                   [×] ││ ← 删除按钮
│ │                                       ││
│ │    [Wide Cover Image Preview]        ││ ← Cover预览
│ │        Full width, 300px高           ││
│ │                                       ││
│ └──────────────────────────────────────┘│
└─────────────────────────────────────────┘
```

**规格**：
- 推荐尺寸：1200x400px 横向图片
- 支持格式：PNG, JPG, GIF, WebP
- 文件大小：最大 5MB
- 显示方式：全宽，cover模式

**验证**：
- ✅ 文件类型检查
- ✅ 文件大小限制
- ✅ 实时预览

---

### FR-W5: Video Link (YouTube) 🔶 **可选**

```
┌─────────────────────────────────────────┐
│ Video Link (Optional)                    │
│ ℹ️ Add a YouTube video link...          │
│                                          │
│ 🎥 [https://www.youtube.com/watch?v=...]│ ← 带图标输入
│                                          │
│ ✓ Valid YouTube URL                     │ ← 验证提示
└─────────────────────────────────────────┘
```

**字段**：
- 类型：URL输入框
- 必填：否
- 图标：YouTube播放图标
- 验证：YouTube URL格式

**支持格式**：
- `https://www.youtube.com/watch?v=...`
- `https://youtu.be/...`
- `http://www.youtube.com/...`

**验证逻辑**：
```typescript
const youtubeRegex = /^(https?:\/\/)?(www\.)?(youtube\.com|youtu\.be)\/.+$/;
```

**特点**：
- ✅ 实时格式验证
- ✅ 有效URL显示绿色对勾
- ✅ 无效URL显示错误提示
- ✅ 左侧YouTube图标

---

## 🎨 UI/UX 设计

### 整体布局

```
┌──────────────────────────────────────────────┐
│                                               │
│  【Job Details】标题                         │
│  描述文字                                    │
│                                               │
│  ┌─────────────────────────────────────────┐│
│  │ Company Name * (FR-W0)                  ││
│  └─────────────────────────────────────────┘│
│                                               │
│  ┌─────────────────────────────────────────┐│
│  │ Job Description * (FR-W1)               ││
│  │ [大型文本区域 - 12行]                   ││
│  └─────────────────────────────────────────┘│
│                                               │
│  ┌─────────────────────────────────────────┐│
│  │ Job Summary * (FR-W2)                   ││
│  │ [文本区域 - 3行，250字符限制]          ││
│  └─────────────────────────────────────────┘│
│                                               │
│  【Key Selling Points】(FR-W3)              │
│  ┌────────┐ ┌────────┐ ┌────────┐         │
│  │ Point 1│ │ Point 2│ │ Point 3│         │
│  └────────┘ └────────┘ └────────┘         │
│                                               │
│  【Company Branding】(FR-W4)                │
│  ┌─────────────────────────────────────────┐│
│  │ Logo上传区域或预览                      ││
│  └─────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────┐│
│  │ Cover上传区域或预览                     ││
│  └─────────────────────────────────────────┘│
│                                               │
│  ┌─────────────────────────────────────────┐│
│  │ 🎥 Video Link (FR-W5)                   ││
│  └─────────────────────────────────────────┘│
│                                               │
│  [Back]                        [Continue] → │
└──────────────────────────────────────────────┘
```

---

### 视觉层次

#### 1. 标题层次
```css
h2.stepTitle {
  font-size: 28px;
  font-weight: 700;
  color: #252525;
}

h3.sectionTitle {
  font-size: 18px;
  font-weight: 700;
  color: #252525;
}

label.label {
  font-size: 15px;
  font-weight: 600;
  color: #252525;
}
```

#### 2. 分区设计
```css
.formSection {
  margin-bottom: 40px;
  padding: 32px;
  background: white;
  border-radius: 12px;
  border: 1px solid #e0e0e0;
}
```

**特点**：
- 白色卡片背景
- 圆角 12px
- 淡灰色边框
- 40px 垂直间距

---

### 交互元素

#### 上传区域（未上传）

```css
.uploadArea {
  border: 2px dashed #e0e0e0;
  border-radius: 12px;
  padding: 40px 20px;
  background: #fafafa;
  cursor: pointer;
  
  悬停时:
    border-color: #4a5bf4;
    background: rgba(74, 91, 244, 0.02);
}
```

**内容**：
- 📤 上传图标（48x48）
- "Click to upload" 文字
- "or drag and drop" 提示

---

#### 图片预览（已上传）

**Logo预览**：
```css
.logoPreviewImage {
  width: 200px;
  height: 200px;
  object-fit: contain;
  background: white;
  padding: 10px;
  border-radius: 8px;
  margin: 20px auto;
}
```

**Cover预览**：
```css
.coverPreviewImage {
  width: 100%;
  height: 300px;
  object-fit: cover;
}
```

**删除按钮**：
```css
.removeImageBtn {
  position: absolute;
  top: 16px;
  right: 16px;
  background: rgba(255, 255, 255, 0.95);
  color: #e74c3c;
  backdrop-filter: blur(10px);
  
  悬停时:
    background: #e74c3c;
    color: white;
    transform: translateY(-2px);
}
```

---

## 🔒 表单验证

### 必填字段验证

```typescript
const validateStep2 = (): boolean => {
  const newErrors: Record<string, string> = {};
  
  // 1. Company Name - 必填
  if (!formData.companyName.trim()) {
    newErrors.companyName = 'Company name is required';
  }
  
  // 2. Job Description - 必填
  if (!formData.jobDescription.trim()) {
    newErrors.jobDescription = 'Job description is required';
  }
  
  // 3. Job Summary - 必填
  if (!formData.jobSummary.trim()) {
    newErrors.jobSummary = 'Job summary is required';
  }
  
  // 4. YouTube URL - 可选，但格式必须正确
  if (formData.videoLink.trim()) {
    const youtubeRegex = /^(https?:\/\/)?(www\.)?(youtube\.com|youtu\.be)\/.+$/;
    if (!youtubeRegex.test(formData.videoLink)) {
      newErrors.videoLink = 'Please enter a valid YouTube URL';
    }
  }
  
  setErrors(newErrors);
  return Object.keys(newErrors).length === 0;
};
```

---

### 文件上传验证

#### Logo上传
```typescript
const handleLogoUpload = (e: React.ChangeEvent<HTMLInputElement>) => {
  const file = e.target.files?.[0];
  if (file) {
    // 1. 验证文件类型
    if (!file.type.startsWith('image/')) {
      setErrors(prev => ({ ...prev, companyLogo: 'Please upload an image file' }));
      return;
    }
    
    // 2. 验证文件大小（最大5MB）
    if (file.size > 5 * 1024 * 1024) {
      setErrors(prev => ({ ...prev, companyLogo: 'Image size must be less than 5MB' }));
      return;
    }
    
    // 3. 生成预览
    const reader = new FileReader();
    reader.onloadend = () => {
      setLogoPreview(reader.result as string);
      setFormData(prev => ({ ...prev, companyLogo: reader.result as string }));
    };
    reader.readAsDataURL(file);
    
    // 4. 清除错误
    setErrors(prev => {
      const newErrors = { ...prev };
      delete newErrors.companyLogo;
      return newErrors;
    });
  }
};
```

#### Cover上传（相同逻辑）
```typescript
const handleCoverUpload = (e: React.ChangeEvent<HTMLInputElement>) => {
  // 与Logo上传相同的验证逻辑
};
```

---

### 字符限制

#### Job Summary
```typescript
<textarea
  maxLength={250}  // 硬限制250字符
  value={formData.jobSummary}
  onChange={(e) => setFormData(prev => ({ ...prev, jobSummary: e.target.value }))}
/>
<div className={styles.charCount}>
  {formData.jobSummary.length} / 250 characters
</div>
```

---

## 📋 数据结构

### JobFormData 接口（Step 2部分）

```typescript
interface JobFormData {
  // ... Step 1 fields ...
  
  // Step 2: Write
  companyName: string;              // 公司名称 *
  jobDescription: string;           // 职位描述 *
  jobSummary: string;               // 职位摘要 *
  keySellingPoint1: string;         // 卖点1 (可选)
  keySellingPoint2: string;         // 卖点2 (可选)
  keySellingPoint3: string;         // 卖点3 (可选)
  companyLogo: string;              // Logo (base64)
  companyCoverImage: string;        // Cover (base64)
  videoLink: string;                // YouTube链接 (可选)
  
  // ... Step 3 fields ...
}
```

---

### 初始值

```typescript
const [formData, setFormData] = useState<JobFormData>({
  // ...
  companyName: '',
  jobDescription: '',
  jobSummary: '',
  keySellingPoint1: '',
  keySellingPoint2: '',
  keySellingPoint3: '',
  companyLogo: '',
  companyCoverImage: '',
  videoLink: '',
  // ...
});
```

---

## 🎯 用户体验设计

### 1. 渐进式披露

```
必填字段 (顶部)
  ↓
  Company Name *
  Job Description *
  Job Summary *
  
可选字段 (中部)
  ↓
  Key Selling Points (3个)
  
可选增强 (底部)
  ↓
  Company Branding
  Video Link
```

**原则**：重要信息在前，可选内容在后

---

### 2. 清晰的视觉反馈

#### 必填标记
```html
<label>
  Company Name <span class="required">*</span>
</label>
```

#### 错误提示
```html
<input class="inputError" />
<span class="errorText">Company name is required</span>
```

#### 成功提示
```html
<div class="videoPreviewHint">
  ✓ Valid YouTube URL
</div>
```

---

### 3. 智能提示

#### 字段说明
```
Job Description *
ℹ️ Provide a detailed description of the role, responsibilities, and requirements
```

#### 占位符示例
```
e.g., Join our AI team to build cutting-edge machine learning solutions...
```

#### 推荐规格
```
Recommended: Square image, at least 200x200px (PNG, JPG, max 5MB)
```

---

### 4. 实时反馈

| 功能 | 反馈方式 |
|------|----------|
| 字符计数 | 实时显示剩余字符 |
| 文件上传 | 立即显示预览 |
| URL验证 | 即时显示有效/无效 |
| 必填验证 | 点击Continue时检查 |

---

## 🔄 交互流程

### 场景 1: 填写基本信息

```
步骤 1: 输入公司名称
  [StarPlan Technologies_________]
  
步骤 2: 编写职位描述
  [We are looking for a talented ML Engineer...
   (详细描述 2000+ 字符)]
   
步骤 3: 编写职位摘要
  [Join our AI team to build cutting-edge solutions
   that impact millions of users worldwide.]
   187 / 250 characters
   
✅ 必填信息完成
```

---

### 场景 2: 添加卖点

```
步骤 1: 输入3个卖点
  Selling Point 1: [Competitive salary + equity____]
  Selling Point 2: [Work on cutting-edge AI projects]
  Selling Point 3: [Flexible remote work policy____]
  
✅ 使职位更有吸引力
```

---

### 场景 3: 上传品牌资料

```
步骤 1: 上传Logo
  点击上传区域
  → 选择文件
  → 验证文件（类型、大小）
  → 显示预览
  ✅ Logo已上传
  
步骤 2: 上传Cover
  点击上传区域
  → 选择文件
  → 验证文件（类型、大小）
  → 显示预览
  ✅ Cover已上传
```

---

### 场景 4: 添加视频

```
步骤 1: 粘贴YouTube链接
  [https://www.youtube.com/watch?v=dQw4w9WgXcQ]
  
步骤 2: 实时验证
  ✓ Valid YouTube URL  ← 绿色提示
  
✅ 视频链接已添加
```

---

### 场景 5: 验证并继续

```
点击 [Continue] 按钮
  ↓
validateStep2() 执行
  ↓
检查必填字段:
  ✓ Company Name: ✅
  ✓ Job Description: ✅
  ✓ Job Summary: ✅
  ✓ Video Link: ✅ (格式正确)
  ↓
所有验证通过
  ↓
进入 Step 3 →
```

---

## 🎨 响应式设计

### 桌面 (>1024px)

```
┌────────────────────────────────────────┐
│ Selling Points (3列网格)              │
│ [Point 1] [Point 2] [Point 3]         │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ Logo预览: 200x200 居中                 │
│ Cover预览: 全宽 300px高               │
└────────────────────────────────────────┘
```

---

### 平板 (768px-1024px)

```
┌────────────────────────────────────────┐
│ Selling Points (2列网格)              │
│ [Point 1] [Point 2]                    │
│ [Point 3]                              │
└────────────────────────────────────────┘
```

---

### 移动 (<768px)

```
┌────────────────────────────────────────┐
│ Selling Points (1列)                   │
│ [Point 1]                              │
│ [Point 2]                              │
│ [Point 3]                              │
└────────────────────────────────────────┘
```

---

## 📊 字段优先级

| 字段 | 优先级 | 必填 | 说明 |
|------|--------|------|------|
| Company Name | P0 | ✅ | 必填，显示在职位顶部 |
| Job Description | P0 | ✅ | 核心内容 |
| Job Summary | P0 | ✅ | 搜索摘要 |
| Key Selling Points | P1 | ❌ | 增强吸引力 |
| Company Logo | P2 | ❌ | 品牌展示 |
| Cover Image | P2 | ❌ | 视觉吸引 |
| Video Link | P2 | ❌ | 额外媒体 |

---

## 🧪 测试场景

### 测试 1: 必填字段验证
```
操作: 直接点击 Continue（不填任何字段）
预期: 
  - 显示3个错误提示
  - Company name is required
  - Job description is required
  - Job summary is required
结果: ✅ 通过
```

### 测试 2: 字符限制
```
操作: Job Summary输入超过250字符
预期: 
  - 阻止输入第251个字符
  - 显示 "250 / 250 characters"
结果: ✅ 通过
```

### 测试 3: Logo上传验证
```
操作: 上传非图片文件（PDF）
预期: 
  - 显示错误："Please upload an image file"
  - 不生成预览
结果: ✅ 通过
```

### 测试 4: 文件大小限制
```
操作: 上传10MB图片
预期: 
  - 显示错误："Image size must be less than 5MB"
  - 不生成预览
结果: ✅ 通过
```

### 测试 5: YouTube URL验证
```
操作: 输入无效URL "https://vimeo.com/123"
预期: 
  - 点击Continue时显示错误
  - "Please enter a valid YouTube URL"
结果: ✅ 通过
```

### 测试 6: 删除已上传图片
```
操作: 上传Logo后点击删除按钮
预期: 
  - 预览消失
  - 恢复上传区域
  - formData.companyLogo清空
结果: ✅ 通过
```

---

## 🎊 总结

### ✅ 已实现的功能

1. ✅ **FR-W0**: Company Name（必填）
2. ✅ **FR-W1**: Job Description（富文本编辑器）
3. ✅ **FR-W2**: Job Summary（简短描述 + 字符限制）
4. ✅ **FR-W3**: Key Selling Points（3个可选输入）
5. ✅ **FR-W4**: Company Branding
   - Logo上传 + 预览 + 删除
   - Cover上传 + 预览 + 删除
6. ✅ **FR-W5**: Video Link（YouTube验证）

### 📈 技术特点

- ✅ TypeScript 类型安全
- ✅ 完整的表单验证
- ✅ 文件上传预览
- ✅ 实时字符计数
- ✅ 响应式布局
- ✅ 清晰的错误提示
- ✅ Base64图片存储
- ✅ 0错误编译

### 🎨 UI/UX 亮点

- 🎯 渐进式信息披露
- 💡 智能提示和示例
- ✨ 实时反馈
- 🔒 客户端验证
- 📱 响应式设计
- 🖼️ 即时图片预览
- ✓ 成功状态提示

---

## 📝 数据示例

### 完整的Step 2数据

```typescript
{
  companyName: "StarPlan Technologies",
  jobDescription: "We are seeking an experienced Machine Learning Engineer to join our AI team...",
  jobSummary: "Join our AI team to build cutting-edge machine learning solutions that impact millions of users worldwide.",
  keySellingPoint1: "Competitive salary + equity package",
  keySellingPoint2: "Work on cutting-edge AI/ML projects",
  keySellingPoint3: "Flexible remote work policy",
  companyLogo: "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgA...",
  companyCoverImage: "data:image/jpeg;base64,/9j/4AAQSkZJRgABAQAA...",
  videoLink: "https://www.youtube.com/watch?v=dQw4w9WgXcQ"
}
```

---

**Page 2 - Write 完整实现完成！** 🎉

用户可以轻松填写职位详情，上传品牌资料，创建专业且有吸引力的职位广告。













