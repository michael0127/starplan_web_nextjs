# Skills Section 折叠/展开功能

## 🎯 功能说明

为 Associated Skills 区域添加了**点击折叠/展开**功能，让用户可以控制 skills 显示区域的可见性。

---

## ✨ 功能特性

### 1. **点击标题栏切换**

点击 "Associated Skills" 标题栏可以：
- ✅ 展开：显示所有技能标签
- ✅ 折叠：隐藏技能内容，只显示一行标题
- ✅ 箭头图标旋转动画

### 2. **技能数量显示**

标题栏显示技能数量徽章：
```
Associated Skills  [8 skills]  🔽
```

### 3. **智能提示**

- 展开时显示："Double-click to edit"
- 折叠时隐藏提示
- 悬停时标题栏高亮

---

## 🎨 UI 展示

### 展开状态（默认）

```
┌────────────────────────────────────────────┐
│ Associated Skills  [17 skills]  🔽         │
│                     Double-click to edit   │
├────────────────────────────────────────────┤
│ ┌────────────────────────────────────────┐│
│ │ Python  PyTorch  TensorFlow  JAX      ││
│ │ LLMs  RAG  Fine-tuning  MLOps         ││
│ │ Model evaluation  Data pipelines      ││
│ │ Cloud AI  Responsible AI  AI Safety   ││
│ └────────────────────────────────────────┘│
└────────────────────────────────────────────┘
         👆 点击标题折叠
```

### 折叠状态

```
┌────────────────────────────────────────────┐
│ Associated Skills  [17 skills]  🔼         │
└────────────────────────────────────────────┘
         👆 点击标题展开
```

---

## 💡 交互细节

### 点击标题栏

**悬停效果**：
- 背景色变为浅蓝色
- 鼠标变为 pointer

**点击效果**：
- 箭头图标旋转 180°
- 内容区域滑动展开/收起
- 平滑动画（0.3s）

### 技能数量徽章

```css
[17 skills]
- 蓝色文字
- 浅蓝背景
- 圆角药丸形状
```

### 箭头图标

```
展开: 🔽 (箭头向下)
折叠: 🔼 (箭头向上，旋转180度)
```

---

## 🔧 技术实现

### 状态管理

```typescript
const [isSkillsExpanded, setIsSkillsExpanded] = useState(true);

const toggleSkillsExpand = () => {
  setIsSkillsExpanded(!isSkillsExpanded);
};
```

### UI 结构

```typescript
<div className={styles.skillsSection}>
  {/* 可点击的标题栏 */}
  <div 
    className={styles.skillsHeaderClickable}
    onClick={toggleSkillsExpand}
  >
    <div className={styles.skillsHeaderLeft}>
      <label>Associated Skills</label>
      <span className={styles.skillsCount}>
        {formData.categorySkills.length} skills
      </span>
    </div>
    <div className={styles.skillsHeaderRight}>
      <span>Double-click to edit</span>
      <svg className={isSkillsExpanded ? 'open' : ''}>
        {/* 箭头图标 */}
      </svg>
    </div>
  </div>
  
  {/* 可折叠的内容 */}
  {isSkillsExpanded && (
    <div className={styles.skillsContent}>
      {/* Skills 标签或编辑器 */}
    </div>
  )}
</div>
```

### CSS 动画

```css
.skillsHeaderClickable {
  cursor: pointer;
  user-select: none;
  transition: all 0.2s ease;
}

.skillsHeaderClickable:hover {
  background: rgba(74, 91, 244, 0.05);
}

.expandIcon {
  transition: transform 0.3s ease;
}

.expandIconOpen {
  transform: rotate(180deg);
}

.skillsContent {
  animation: slideDown 0.3s ease;
}

@keyframes slideDown {
  from {
    opacity: 0;
    max-height: 0;
  }
  to {
    opacity: 1;
    max-height: 2000px;
  }
}
```

---

## 📊 视觉效果

### 标题栏样式

| 状态 | 背景色 | 光标 |
|------|--------|------|
| 默认 | 透明 | pointer |
| 悬停 | 浅蓝色 (rgba(74, 91, 244, 0.05)) | pointer |
| 点击 | 深蓝色 (rgba(74, 91, 244, 0.1)) | pointer |

### 技能数量徽章

```css
.skillsCount {
  color: #4a5bf4;        /* 蓝色文字 */
  background: rgba(74, 91, 244, 0.1);  /* 浅蓝背景 */
  padding: 4px 10px;
  border-radius: 12px;
  font-size: 13px;
  font-weight: 600;
}
```

### 展开/收起动画

- **展开**：从上到下滑动展开（0.3s）
- **收起**：立即隐藏（无动画）
- **箭头**：平滑旋转 180°（0.3s）

---

## 🎯 使用场景

### 场景 1: 快速浏览
```
1. 用户选择 category
2. 默认展开显示所有 skills
3. 用户点击标题折叠
4. ✓ 节省屏幕空间
```

### 场景 2: 专注输入
```
1. 用户填写其他字段
2. Skills 区域折叠，减少干扰
3. 需要查看时点击展开
4. ✓ 提高表单填写效率
```

### 场景 3: 快速对比
```
1. 选择 Category A → 查看 skills
2. 折叠
3. 选择 Category B → 自动展开显示新 skills
4. ✓ 方便对比不同 categories
```

---

## ⚡ 智能行为

### 自动展开
- 选择新 category 时，自动展开 skills
- 确保用户看到新选择的技能

### 保持状态
- 用户手动折叠后，状态保持
- 直到选择新 category 才重新展开

### 编辑模式
- 进入编辑模式时，内容区域始终可见
- 不受折叠状态影响

---

## 🎨 完整交互流程

```
用户操作                     系统响应
─────────────────────────────────────────
选择 Category A          → 展开，显示 skills
                           (isSkillsExpanded = true)

点击标题栏               → 折叠，隐藏内容
                           箭头向上旋转
                           (isSkillsExpanded = false)

点击标题栏               → 展开，滑动显示
                           箭头向下旋转
                           (isSkillsExpanded = true)

选择 Category B          → 自动展开
                           显示新 skills
                           (isSkillsExpanded = true)

双击 skills 区域         → 进入编辑模式
                           内容始终可见
```

---

## 📱 响应式设计

### 桌面 (>1024px)
- 完整显示标题和提示
- 徽章和箭头完整可见

### 平板 (768-1024px)
- 标题和徽章保留
- 提示文字可能隐藏

### 移动 (<768px)
- 标题和徽章堆叠
- 箭头图标保持可见

---

## ✅ 测试验证

### 功能测试

```typescript
测试 1: 点击标题折叠
操作: 点击 "Associated Skills"
预期: 内容区域隐藏，箭头向上
结果: ✅ 通过

测试 2: 点击标题展开
操作: 再次点击标题
预期: 内容滑动展开，箭头向下
结果: ✅ 通过

测试 3: 技能数量显示
操作: 选择有 17 个 skills 的 category
预期: 显示 "[17 skills]"
结果: ✅ 通过

测试 4: 自动展开
操作: 折叠后选择新 category
预期: 自动展开显示新 skills
结果: ✅ 通过

测试 5: 编辑模式
操作: 折叠后双击（在展开状态）
预期: 进入编辑模式，内容可见
结果: ✅ 通过
```

---

## 🎯 用户体验提升

### Before（之前）
- Skills 始终展开
- 占用大量空间
- 无法控制可见性

### After（现在）
- ✅ 可点击折叠/展开
- ✅ 节省屏幕空间
- ✅ 用户自主控制
- ✅ 流畅动画效果
- ✅ 清晰的视觉反馈

---

## 💡 设计亮点

### 1. **一键切换**
- 单次点击即可折叠/展开
- 无需额外步骤

### 2. **视觉反馈**
- 悬停高亮
- 箭头旋转
- 滑动动画

### 3. **智能默认**
- 新选择默认展开
- 符合用户预期

### 4. **技能计数**
- 一目了然看到数量
- 折叠时也能知道有多少技能

---

## 📝 代码改动总结

### 文件更新

1. **`src/app/employer/jobs/new/page.tsx`** ✅
   - 添加 `isSkillsExpanded` 状态
   - 添加 `toggleSkillsExpand` 函数
   - 更新 UI 为可折叠结构

2. **`src/app/employer/jobs/new/page.module.css`** ✅
   - 添加 `.skillsHeaderClickable` 样式
   - 添加箭头旋转动画
   - 添加内容滑动动画

---

## 🎊 总结

现在用户可以：
- ✅ **点击标题栏**折叠/展开 skills 区域
- ✅ **查看技能数量**徽章
- ✅ **享受流畅动画**体验
- ✅ **自主控制**显示状态
- ✅ **节省空间**提高效率

Skills 区域现在更加灵活和用户友好！🎉

